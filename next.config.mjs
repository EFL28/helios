/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "crests.football-data.org",
      "flagcdn.com",
      "media.formula1.com",
      "assets.laliga.com",
    ],
  },
};

export default nextConfig;
